import { Component } from '@angular/core';

// Stub of Header component. Allows shallow testing of components that import header
@Component({
    selector: 'app-header',
    template: ``,
    styleUrls: ['./header.component.scss']})
  export class HeaderStubComponent {
  
}